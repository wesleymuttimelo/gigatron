# Gigatron Erp

Just employee crud for a test.

## Installation

clone this repository 
create a file .env with .env.example
run php artisan migrate
run npm install
run npm dev
run php artisan serve

